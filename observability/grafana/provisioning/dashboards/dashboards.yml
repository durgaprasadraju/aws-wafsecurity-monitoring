apiVersion: 1

providers:
  - name: waf-dashboards
    orgId: 1
    folder: WAF Security
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
